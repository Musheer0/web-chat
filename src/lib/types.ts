export type WebsiteMetadata = {
  title: string; // name of the page. like book title.
  description: string; // summary in 1 sentence. like back cover of book.
  favicon: string; // tiny icon URL top of browser tab
  banner: string; // big preview image (social share / OG)
  language: string; // which language page is in. like "en" or "hi"
}
